import { makeStyles } from "@material-ui/core";

export default makeStyles(() =>({
    CircularProgress: {
        position: 'relative',
        margin: '30px',
        display: 'flex',
        flexFlow: 'column wrap',
        alignItems: 'center',
    }
}));